# 校园论坛后端接口文档

> 基于代码实际实现编写，所有字段名与 VO/DTO 完全一致

## 概览

- **基础路径**：`http://localhost:8090`
- **认证方式**：JWT Token，通过请求头 `token` 字段传递
- **拦截范围**：`/admin/**` 路径需要 Token（登录/注册除外）
- **统一响应格式**：

```json
{
  "code": 1,       // 1=成功, 0=失败
  "msg": "操作成功",
  "data": {}        // 具体数据
}
```

---

## 一、用户模块 (`/admin/user`)

### 1.1 用户登录

- **POST** `/admin/user/login`
- **无需认证**

**请求体** (`LoginDto`)：
```json
{
  "username": "张三",
  "password": "123456"
}
```

**响应** (`LoginVo`)：
```json
{
  "id": 1,
  "username": "张三",
  "token": "eyJhbGciOiJIUzI1NiJ9...",
  "avatar": "https://xxx/avatar.jpg",
  "email": "zhangsan@example.com"
}
```

---

### 1.2 修改用户信息

- **POST** `/admin/user/change`
- **需要认证**

**请求体** (`User`)：
```json
{
  "username": "新昵称",
  "email": "new@example.com",
  "avatar": "https://xxx/new-avatar.jpg",
  "bio": "个人简介",
  "background": "https://xxx/background.jpg",
  "location": "北京"
}
```

**响应**：`"修改成功"`

---

### 1.3 获取当前用户信息

- **GET** `/admin/user/info`
- **需要认证**

**响应** (`GetUserVO`)：
```json
{
  "id": 1,
  "username": "张三",
  "name": "张三",
  "email": "zhangsan@example.com",
  "bio": "个人简介",
  "avatar": "https://xxx/avatar.jpg",
  "background": "https://xxx/background.jpg",
  "location": "北京",
  "createTime": "2025-01-15",
  "followers": 10,
  "following": 5,
  "articles": 20
}
```

---

### 1.4 获取指定用户信息

- **GET** `/admin/user/info/{id}`
- **需要认证**

**响应** (`GetUserVO`)：同 1.3

---

### 1.5 关注/取消关注

- **POST** `/admin/user/follow`
- **需要认证**
- **Toggle 模式**：调用一次关注，再调用一次取消关注

**请求体** (`FollowDTO`)：
```json
{
  "userId": 2,
  "action": "follow"
}
```
> `action` 字段存在但当前未参与逻辑，实际通过 DB 查询当前状态后取反

**响应**：
```json
{
  "followers": 11
}
```

---

### 1.6 获取关注列表

- **GET** `/admin/user/following`
- **需要认证**

**响应** (`List<FollowUserVO>`)：
```json
[
  {
    "id": 2,
    "username": "李四",
    "avatar": "https://xxx/avatar2.jpg",
    "bio": "李四的简介",
    "isFollowedBack": false
  }
]
```
> `isFollowedBack` 在关注列表中不会被设置（始终为 null/false）

---

### 1.7 获取粉丝列表

- **GET** `/admin/user/followers`
- **需要认证**

**响应** (`List<FollowUserVO>`)：
```json
[
  {
    "id": 3,
    "username": "王五",
    "avatar": "https://xxx/avatar3.jpg",
    "bio": "王五的简介",
    "isFollowedBack": true
  }
]
```
> 粉丝列表中 `isFollowedBack` 会被设置为当前用户是否回关了该粉丝

---

### 1.8 获取当前用户点赞的文章

- **GET** `/admin/user/liked?page=1&pageSize=10`
- **需要认证**

**响应**：
```json
{
  "list": [ /* ArticleVO 数组 */ ],
  "total": 25,
  "page": 1,
  "pageSize": 10
}
```

---

### 1.9 获取指定用户点赞的文章

- **GET** `/admin/user/like/{userId}`
- **需要认证**

**响应** (`List<ArticleVO>`)：
```json
[
  {
    "id": 1,
    "title": "文章标题",
    "content": "文章内容",
    "summary": null,
    "images": ["https://xxx/img1.jpg"],
    "videos": [],
    "cover": "https://xxx/cover.jpg",
    "type": "article",
    "author": { "id": 2, "username": "李四", "avatar": "...", "bio": "...", "followers": 0 },
    "tags": "Java,Spring",
    "likes": 10,
    "comments": 5,
    "views": 100,
    "createTime": "2025-07-01 14:30",
    "isLiked": true,
    "isFollowed": false
  }
]
```

---

### 1.10 搜索用户

- **GET** `/admin/user/search?keyword=关键词`
- **需要认证**

**响应** (`List<UserSearchVO>`)：
```json
[
  {
    "id": 2,
    "username": "李四",
    "avatar": "https://xxx/avatar2.jpg",
    "bio": "李四的简介",
    "isFollowed": false
  }
]
```

---

## 二、文章模块 (`/admin/article`)

### 2.1 文章列表（分页）

- **GET** `/admin/article/list?page=1&pageSize=10`
- **需要认证**（可选，未登录时 `isLiked`/`isFollowed` 为 false）
- **Redis 缓存**：优先从 Redis 读取，缓存未命中时查 DB 并写入缓存（TTL 1天）

**响应** (`List<ArticleVO>`)：
```json
[
  {
    "id": 1,
    "title": "文章标题",
    "content": "文章内容",
    "summary": null,
    "images": ["https://xxx/img1.jpg", "https://xxx/img2.jpg"],
    "videos": ["https://xxx/video1.mp4"],
    "cover": "https://xxx/cover.jpg",
    "type": "article",
    "author": {
      "id": 1,
      "username": "张三",
      "avatar": "https://xxx/avatar.jpg",
      "bio": "个人简介",
      "followers": 0
    },
    "tags": "Java,Spring",
    "likes": 10,
    "comments": 5,
    "views": 100,
    "createTime": "2025-07-01 14:30",
    "isLiked": false,
    "isFollowed": false
  }
]
```
> `tags` 为逗号分隔的字符串，不是数组  
> `createTime` 格式 `yyyy-MM-dd HH:mm`  
> `images`/`videos` 从 `articleimage`/`article_video` 表查询

---

### 2.2 文章详情

- **GET** `/admin/article/{id}`
- **需要认证**（可选）
- **Redis 缓存**：优先 Redis（TTL 1天）

**响应** (`ArticleVO`)：同 2.1 单个对象

---

### 2.3 发布文章

- **POST** `/admin/article/add`
- **需要认证**

**请求体** (`ArticleDTO`)：
```json
{
  "title": "文章标题",
  "content": "文章正文内容",
  "images": "https://xxx/img1.jpg,https://xxx/img2.jpg",
  "videos": "https://xxx/video1.mp4",
  "cover": "https://xxx/cover.jpg",
  "tags": "Java,Spring Boot",
  "type": "article"
}
```
> `images`/`videos` 为逗号分隔的 URL 字符串，不是数组  
> `tags` 为逗号分隔的标签字符串  
> `type` 可选，默认 `"article"`

**响应**：
```json
{
  "id": 1,
  "title": "文章标题",
  "createTime": "2025-07-01 14:30"
}
```

---

### 2.4 修改文章

- **POST** `/admin/article/update`
- **需要认证**
- 只能修改自己发布的文章

**请求体** (`ArticleUpdateDTO`)：
```json
{
  "id": 1,
  "title": "新标题",
  "content": "新内容",
  "images": "https://xxx/new-img1.jpg",
  "videos": "",
  "cover": "https://xxx/new-cover.jpg",
  "tags": "新标签",
  "type": "article"
}
```

**响应**：`"修改成功"`

---

### 2.5 删除文章

- **DELETE** `/admin/article/delete/{id}`
- **需要认证**
- 只能删除自己发布的文章

**响应**：`"删除成功"`

---

### 2.6 增加浏览量

- **POST** `/admin/article/view`
- **需要认证**（可选）
- **Redis**：优先 Redis 递增，Redis 不可用时降级到 DB

**请求体**：
```json
{
  "articleId": 1
}
```

**响应**：
```json
{
  "views": 101
}
```

---

### 2.7 文章点赞/取消点赞

- **POST** `/admin/article/like`
- **需要认证**
- **Toggle 模式**：检查当前状态后反向操作
- **Redis**：使用 Set 存储每篇文章的点赞用户ID集合

**请求体** (`ArticleLikeDTO`)：
```json
{
  "articleId": 1,
  "action": "like"
}
```
> `action` 字段必填（不能为 null），但实际逻辑是 toggle 模式

**响应** (`ArticleLikeVO`)：
```json
{
  "likes": 11
}
```

---

### 2.8 文章收藏/取消收藏

- **POST** `/admin/article/collect`
- **需要认证**
- **Toggle 模式**
- **Redis**：使用 Set 存储每篇文章的收藏用户ID集合

**请求体** (`CollectDTO`)：
```json
{
  "articleId": 1
}
```

**响应**：`"收藏成功"` 或 `"取消收藏"`

---

### 2.9 热门文章（热榜）

- **GET** `/admin/article/hot?limit=10`
- **需要认证**（可选）
- **Redis**：基于近 7 天的每日浏览量统计（`article:views:articleId:DateViews{date}:{articleId}`）

**响应** (`List<ArticleVO>`)：同 2.1 格式，按 7 天总浏览量降序排列

---

### 2.10 随机文章

- **GET** `/admin/article/random?limit=10`
- **需要认证**（可选）
- **Redis**：优先从 ZSet `article:ids` 中随机获取

**响应** (`List<ArticleVO>`)：同 2.1 格式

---

### 2.11 保存草稿

- **POST** `/admin/article/draft`
- **需要认证**
- 每个用户只保留一份草稿（已有则覆盖）

**请求体** (`DraftDTO`)：
```json
{
  "title": "草稿标题",
  "content": "草稿内容",
  "cover": "https://xxx/cover.jpg",
  "tags": "标签1,标签2"
}
```

**响应** (`DraftVO`)：
```json
{
  "id": 1,
  "title": "草稿标题",
  "content": "草稿内容",
  "cover": "https://xxx/cover.jpg",
  "tags": "标签1,标签2",
  "updateTime": "2025-07-01 14:30"
}
```

---

### 2.12 获取草稿

- **GET** `/admin/article/draft`
- **需要认证**

**响应** (`DraftVO`)：同 2.11，无草稿时 `data` 为 null

---

## 三、评论模块

评论有两套路径，功能相同：
- `/admin/article/comment` — 原始路径
- `/admin/comment` — 扩展路径（多了评论点赞）

### 3.1 获取文章评论列表

- **GET** `/admin/article/comment/{articleId}?page=1&pageSize=20`
- 或 **GET** `/admin/comment/list?articleId=1&page=1&pageSize=20`
- **需要认证**（可选）
- **Redis 缓存**：评论数据缓存到 Redis

**响应** (`CommentPageVO`)：
```json
{
  "list": [
    {
      "articleId": 1,
      "id": 10,
      "author": {
        "username": "李四",
        "avatar": "https://xxx/avatar2.jpg",
        "id": 2
      },
      "content": "评论内容",
      "image": "https://xxx/comment-img.jpg",
      "like": 5,
      "createTime": "2025-07-01 15:00",
      "isLiked": false,
      "replies": [
        {
          "id": 11,
          "author": {
            "username": "王五",
            "avatar": "https://xxx/avatar3.jpg",
            "id": 3
          },
          "content": "回复内容",
          "image": "",
          "like": 2,
          "createTime": "2025-07-01 15:30",
          "isLiked": false
        }
      ]
    }
  ],
  "total": 50,
  "page": 1,
  "pageSize": 20
}
```
> `like`（非 `likes`）— 评论点赞数  
> `createTime` 格式 `yyyy-MM-dd HH:mm`

---

### 3.2 发表评论

- **POST** `/admin/article/comment`
- 或 **POST** `/admin/comment/add`
- **需要认证**

**请求体** (`CommentAddDTO`)：
```json
{
  "articleId": 1,
  "content": "评论内容",
  "image": "https://xxx/comment-img.jpg",
  "parentId": null
}
```
> `parentId` 为 null 表示顶级评论，有值表示回复某条评论

**响应**：
```json
{
  "id": 10,
  "image": "https://xxx/comment-img.jpg",
  "create_time": "2025-07-01 15:00"
}
```

---

### 3.3 删除评论

- **DELETE** `/admin/article/comment/delete/{id}`
- 或 **DELETE** `/admin/comment/delete/{id}`
- **需要认证**
- 只能删除自己的评论

**响应**：`"删除成功"`

---

### 3.4 评论点赞/取消点赞

- **POST** `/admin/comment/like`
- **需要认证**
- **Toggle 模式**
- **Redis**：使用 Set 存储每条评论的点赞用户ID集合

**请求体** (`CommentLikeDTO`)：
```json
{
  "commentId": 10,
  "action": "like"
}
```

**响应**：
```json
{
  "likes": 6
}
```

---

## 四、通用模块 (`/admin`)

### 4.1 文件上传

- **POST** `/admin/upload`
- **需要认证**
- **Content-Type**：`multipart/form-data`

**请求参数**：
- `file`：上传的文件

**响应**：
```json
"https://mywebpro.oss-cn-beijing.aliyuncs.com/uuid-filename.jpg"
```
> 返回阿里云 OSS 的文件 URL

---

## 五、热榜模块 (`/admin/hot`)

### 5.1 热榜列表

- **GET** `/admin/hot/list?limit=10`
- **需要认证**（可选）
- 与 2.9 热门文章相同，调用同一 Service 方法

**响应** (`List<ArticleVO>`)：同 2.1 格式

---

## 六、搜索模块 (`/admin`)

### 6.1 综合搜索

- **GET** `/admin/search?q=关键词&type=all&page=1&pageSize=10`
- **需要认证**（可选）

**参数说明**：
| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| q | String | 必填 | 搜索关键词 |
| type | String | "all" | 搜索类型：`all`/`articles`/`users`/`tags` |
| page | int | 1 | 页码 |
| pageSize | int | 10 | 每页数量 |

**响应** (`SearchResultVO`)：
```json
{
  "articles": [
    { /* ArticleVO */ }
  ],
  "users": [
    {
      "id": 2,
      "username": "李四",
      "avatar": "https://xxx/avatar2.jpg",
      "bio": "李四的简介",
      "followers": 10,
      "articles": 5
    }
  ],
  "tags": []
}
```
> `tags` 搜索当前返回空列表（未实现标签搜索）

---

## 七、通知模块 (`/admin/notification`)

### 7.1 获取通知列表

- **GET** `/admin/notification/list?page=1&pageSize=20`
- **需要认证**

**响应** (`List<NotificationVO>`)：
```json
[
  {
    "id": 1,
    "type": "like",
    "user": {
      "id": 2,
      "username": "李四",
      "avatar": "https://xxx/avatar2.jpg"
    },
    "article": {
      "id": 1,
      "title": "文章标题"
    },
    "createTime": "2025-07-01 15:00",
    "isRead": false
  }
]
```

---

### 7.2 标记通知已读

- **POST** `/admin/notification/read`
- **需要认证**

**请求体** (`NotificationReadDTO`)：
```json
{
  "notificationIds": [1, 2, 3]
}
```

**响应**：`"操作成功"`

---

## 八、二手交易模块 (`/admin/trade`)

> **前端已有调用，后端待实现**。以下接口定义根据前端 `api.js` 中的调用路径、mock 数据结构和请求格式整理，后端实现时请按此规范开发。

### 8.1 获取商品列表

- **GET** `/admin/trade/item/list?category={category}&page=1&pageSize=10`
- **需要认证**（可选）
- `category` 参数可选，不传则返回全部商品

**响应** (`List<TradeItemVO>`)：
```json
[
  {
    "id": 1001,
    "title": "九成新 iPad Air 5 256GB",
    "description": "去年买的，平时贴膜带壳使用...",
    "price": 2800,
    "originalPrice": 4799,
    "images": ["https://xxx/img1.jpg", "https://xxx/img2.jpg"],
    "category": "数码电子",
    "condition": "9成新",
    "seller": {
      "id": 1,
      "username": "张三",
      "avatar": "https://xxx/avatar.jpg"
    },
    "views": 342,
    "likes": 28,
    "createTime": "2025-07-01 10:30",
    "location": "东区宿舍",
    "status": "on_sale"
  }
]
```
> `status` 可选值：`on_sale`（在售）、`sold`（已售）、`off_shelf`（已下架）

---

### 8.2 获取商品详情

- **GET** `/admin/trade/item/{id}`
- **需要认证**（可选）

**响应** (`TradeItemVO`)：同 8.1 单个对象

---

### 8.3 发布二手商品

- **POST** `/admin/trade/item/add`
- **需要认证**

**请求体** (`TradeItemDTO`)：
```json
{
  "title": "二手iPad Air 5",
  "description": "9成新，配件齐全",
  "price": 2800,
  "originalPrice": 4799,
  "images": ["https://xxx/img1.jpg", "https://xxx/img2.jpg"],
  "category": "数码电子",
  "condition": "9成新",
  "location": "东区宿舍"
}
```

**响应** (`TradeItemVO`)：
```json
{
  "id": 1010,
  "title": "二手iPad Air 5",
  "createTime": "2025-07-01 14:30",
  "status": "on_sale"
}
```

---

### 8.4 获取交易分类列表

- **GET** `/admin/trade/category/list`
- **需要认证**（可选）
- 当前前端为纯 mock 数据，后端待实现

**响应** (`List<TradeCategoryVO>`)：
```json
[
  { "id": 0, "name": "全部", "icon": "🏷️" },
  { "id": 1, "name": "数码电子", "icon": "📱" },
  { "id": 2, "name": "教材书籍", "icon": "📚" },
  { "id": 3, "name": "服饰鞋包", "icon": "👟" },
  { "id": 4, "name": "运动户外", "icon": "🚲" },
  { "id": 5, "name": "生活日用", "icon": "🏠" },
  { "id": 6, "name": "美妆护肤", "icon": "💄" },
  { "id": 7, "name": "其他", "icon": "📦" }
]
```

---

## 九、跑腿任务模块 (`/admin/errand`)

> **前端已有调用，后端待实现**。以下接口定义根据前端 `api.js` 中的调用路径、mock 数据结构和请求格式整理。

### 9.1 获取任务列表

- **GET** `/admin/errand/task/list?category={category}&page=1&pageSize=10`
- **需要认证**（可选）
- `category` 参数可选，不传则返回全部任务

**响应** (`List<ErrandTaskVO>`)：
```json
[
  {
    "id": 2001,
    "title": "帮取菜鸟驿站快递",
    "description": "菜鸟驿站取一个中等大小的包裹...",
    "reward": 5.00,
    "category": "代取快递",
    "publisher": {
      "id": 1,
      "username": "张三",
      "avatar": "https://xxx/avatar.jpg"
    },
    "runner": null,
    "fromLocation": "菜鸟驿站（校门口）",
    "toLocation": "东区3号楼",
    "deadline": "2025-07-02 18:00",
    "createTime": "2025-07-01 13:00",
    "status": "pending",
    "tags": ["快递代取", "轻松简单"]
  }
]
```
> `status` 可选值：`pending`（待接单）、`accepted`（已接单）、`in_progress`（进行中）、`completed`（已完成）、`cancelled`（已取消）

---

### 9.2 获取任务详情

- **GET** `/admin/errand/task/{id}`
- **需要认证**（可选）

**响应** (`ErrandTaskVO`)：同 9.1 单个对象

---

### 9.3 发布跑腿任务

- **POST** `/admin/errand/task/add`
- **需要认证**

**请求体** (`ErrandTaskDTO`)：
```json
{
  "title": "帮取快递",
  "description": "菜鸟驿站取一个包裹",
  "reward": 5.00,
  "category": "代取快递",
  "fromLocation": "菜鸟驿站",
  "toLocation": "东区3号楼502",
  "deadline": "2025-07-02 18:00",
  "tags": ["快递代取"]
}
```

**响应** (`ErrandTaskVO`)：
```json
{
  "id": 2010,
  "title": "帮取快递",
  "createTime": "2025-07-01 14:30",
  "status": "pending"
}
```

---

### 9.4 接单跑腿任务

- **POST** `/admin/errand/task/accept`
- **需要认证**
- 只能接别人发布的、状态为 `pending` 的任务

**请求体**：
```json
{
  "taskId": 2001
}
```

**响应** (`ErrandTaskVO`)：同 9.1，`runner` 字段填充为当前用户，`status` 变为 `accepted`

---

### 9.5 获取跑腿分类列表

- **GET** `/admin/errand/category/list`
- **需要认证**（可选）
- 当前前端为纯 mock 数据，后端待实现

**响应** (`List<ErrandCategoryVO>`)：
```json
[
  { "id": 0, "name": "全部", "icon": "📋" },
  { "id": 1, "name": "代取快递", "icon": "📦" },
  { "id": 2, "name": "代买餐食", "icon": "🍜" },
  { "id": 3, "name": "代办事务", "icon": "✅" },
  { "id": 4, "name": "搬运帮忙", "icon": "📦" },
  { "id": 5, "name": "其他", "icon": "🔧" }
]
```

---

## 十、私信/消息模块 (`/admin/message`)

> **前端已有调用，后端待实现**。以下接口定义根据前端 `api.js` 中的调用路径和请求格式整理，后端实现时请按此规范开发。

### 8.1 获取会话列表

- **GET** `/admin/message/conversations`
- **需要认证**
- 返回当前用户的所有私信会话，按最后消息时间倒序

**响应** (`List<ConversationVO>`)：
```json
[
  {
    "id": 1,
    "peerUser": {
      "id": 3,
      "username": "李四",
      "avatar": "https://xxx/avatar3.jpg"
    },
    "lastMessage": "你好，请问还在吗？",
    "lastTime": "2025-07-01 15:30",
    "unreadCount": 2
  }
]
```

---

### 8.2 获取消息记录

- **GET** `/admin/message/history/{conversationId}?page=1&pageSize=20`
- **需要认证**
- 返回指定会话的消息记录，按时间正序

**响应** (`List<MessageVO>`)：
```json
[
  {
    "id": 1,
    "conversationId": 1,
    "sender": {
      "id": 2,
      "username": "张三",
      "avatar": "https://xxx/avatar.jpg"
    },
    "content": "你好",
    "createTime": "2025-07-01 15:00",
    "isRead": true
  }
]
```

---

### 8.3 发送私信

- **POST** `/admin/message/send`
- **需要认证**
- 如果 `conversationId` 为空，则自动创建新会话

**请求体** (`MessageSendDTO`)：
```json
{
  "conversationId": 1,
  "content": "你好，请问还在吗？"
}
```

**响应** (`MessageVO`)：
```json
{
  "id": 10,
  "conversationId": 1,
  "sender": {
    "id": 2,
    "username": "张三",
    "avatar": "https://xxx/avatar.jpg"
  },
  "content": "你好，请问还在吗？",
  "createTime": "2025-07-01 15:30",
  "isRead": false
}
```

---

### 8.4 获取未读消息数

- **GET** `/admin/message/unread`
- **需要认证**
- 返回当前用户所有未读消息的总数

**响应**：
```json
{
  "count": 5
}
```

---

## 十一、Redis 缓存策略

| 缓存项 | Key 格式 | 类型 | TTL | 说明 |
|--------|----------|------|-----|------|
| 文章缓存 | `article:{id}` | String (ArticleVO) | 1天 | 文章详情/列表缓存 |
| 文章ID集合 | `article:ids` | ZSet | 无 | 评分为发布时间戳，用于随机文章 |
| 文章点赞 | `article:likes:articleId:{id}` | Set | 无 | 成员为用户ID |
| 文章收藏 | `article:collects:articleId:{id}` | Set | 无 | 成员为用户ID |
| 文章总浏览量 | `article:views:articleId:{id}` | String (Integer) | 7天 | 增量计数器 |
| 文章每日浏览量 | `article:views:articleId:DateViews{date}:{id}` | String (Integer) | 8天 | 按日统计 |
| 评论缓存 | `article:comment:articleId:{id}` | String (listVO) | 无 | 评论对象缓存 |
| 评论点赞 | `article:comment:likes:articleId:{id}` | Set | 无 | 成员为用户ID |
| 评论点赞数 | `article:comment:collects:likes:{id}` | String (Integer) | 无 | 点赞计数 |
| 用户信息 | `user:{id}` | String (GetUserVO) | 无 | 用户资料缓存 |
| 用户关注 | `user:follows:{userId}` | Set | 无 | 成员为被关注用户ID |

### 定时同步任务（每分钟执行）

| 任务 | 说明 |
|------|------|
| `syncArticleLikes` | Redis 点赞 Set → DB `user_like` 表 + 更新 `article.like_count` |
| `syncArticleCollects` | Redis 收藏 Set → DB `user_collect` 表 |
| `syncArticleViews` | Redis 浏览量计数器 → DB `article.view_count` |
| `syncCommentLikes` | Redis 评论点赞 Set → DB `comment.likes` 字段 |
| `cleanExpiredArticleCache` | 每小时清理已删除文章的缓存（TTL 1小时） |

---

## 十二、数据库表结构

| 表名 | 说明 |
|------|------|
| `users` | 用户表 |
| `article` | 文章表 |
| `comment` | 评论表 |
| `articleimage` | 文章图片表 |
| `article_video` | 文章视频表 |
| `user_like` | 用户点赞表 |
| `user_follow` | 用户关注表 |
| `user_collect` | 用户收藏表 |
| `notification` | 通知表 |
| `draft` | 草稿表 |
| `message` | 私信消息表 |
| `conversation` | 私信会话表 |
| `message_read` | 消息已读记录表 |
| `trade_item` | 二手商品表 |
| `trade_category` | 交易分类表 |
| `errand_task` | 跑腿任务表 |
| `errand_category` | 跑腿分类表 |
